# The upstream IPv6 route may be advertised before it is actually usable.
# Package downloads must fail over promptly instead of holding the LuCI RPC
# request open until the browser receives an incomplete JSON response.
inet4_only = on
