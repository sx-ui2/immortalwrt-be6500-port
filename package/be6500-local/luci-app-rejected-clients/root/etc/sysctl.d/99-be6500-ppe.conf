# PPP interfaces inherit the system default qdisc. Qualcomm PPE rejects an
# fq_codel qdisc on pppoe-wan; noqueue lets the hardware scheduler on the
# underlying physical WAN port own queueing and shaping.
net.core.default_qdisc=noqueue
