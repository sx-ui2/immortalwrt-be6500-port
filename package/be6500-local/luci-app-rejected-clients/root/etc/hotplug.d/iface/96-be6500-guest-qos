#!/bin/sh

# Rebuild HTB on the current WAN L3 device after DHCP/PPPoE reconnects. This
# only changes traffic-control state; it never restarts a radio.
case "$ACTION:$INTERFACE" in
	ifup:guest|ifup:lan|ifup:wan|ifup:wan6) /usr/sbin/be6500-qos-apply >/dev/null 2>&1 & ;;
esac
